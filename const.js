let height = 500;
let width = 800;

function random(min, max) {
    return Math.floor(Math.random() * (max - min));
}

let time = 60;
let start = false

let sumMate = 0;
let sumSights = 0;
let arrTVCollected = [];
let arrBranCollected = [];
let sumKebab = 0;


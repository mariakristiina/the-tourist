let height = 850;
let width = 1600;

function random(min, max) {
    return Math.floor(Math.random() * (max - min));
}

let time = 30;
let start = false

let sumMate = 0;

let sumSights = 0;
let sightsCollected = [];

let sumKebab = 0;

let hospitalCollected = [];

let sumFood= 0;

let sumClub = 0;